# 005｜大脑的类最小作用量原理：有限资源下的认知路径优化

**作者：** zq

---

## 摘要

本文提出一种“类最小作用量”的认知模型：大脑并不是在无限计算中寻找绝对真理，而是在有限能量、有限注意力、有限时间和有限记忆容量下，选择一条能够稳定降低未来不确定性、支持行动并维持自我-环境耦合的低代价路径。

严格地说，本文并不主张真实生物大脑在物理力学意义上遵守最小作用量原理，而是提出一个资源约束主动推理框架。在该框架中，预测编码提供误差抵消机制，神经放电积分提供微观动力学基础，突触可塑性和动态阈值把重复误差转化为先验，变分自由能提供瞬时代价函数，资源约束形成跨时间路径代价，多模型贝叶斯竞争解释候选解释之间的动态选择，记忆动力学保存可复用的低代价路径，而高阶观察者则被形式化为调节成本权重、精度、更新增益和有效温度的元控制系统。

---

## 1. 问题的出发点

经典物理中的最小作用量原理告诉我们：系统的真实演化路径并不是任意发生的，而是在某种总体约束下，使作用量达到驻定或最优的路径。

如果将这个思想迁移到认知系统中，我们可以提出一个类似但不等同的命题：

> 大脑不是无限计算系统，而是在有限资源下选择一条足够稳定、足够低成本、足够能解释世界并支持行动的认知路径。

这里的“作用量”不再是物理中的动能减势能积分，而是认知系统在时间中累计付出的总代价。它包括预测误差、神经能耗、注意力负担、记忆维护成本、高阶控制成本和行动成本。

因此，大脑可以被理解为一种**类最小作用量系统**。更准确地说：

> 大脑的智能不是无限计算，而是有限资源下的路径选择。

---

## 2. 与既有理论的关系

本文并不是要替代已有的有限理性、认知控制、压缩学习或主动推理理论，而是试图用一个统一的路径代价泛函把它们连接起来。

第一，本文的有限资源假设与 **resource-rational analysis** 高度相关。Lieder 和 Griffiths 的资源理性分析把人类认知理解为有限计算资源下对认知操作的最优使用。本文继承这一精神，但把时间、注意力、记忆和控制成本嵌入主动推理的跨时间路径泛函中。

第二，本文对高阶控制成本的处理与 Shenhav、Botvinick 和 Cohen 提出的 **Expected Value of Control** 理论高度一致。EVC 理论认为，控制投入取决于预期收益、所需控制强度和认知努力成本。本文中的 $\lambda_C C_t$ 与元控制变量 $\theta_t$ 可以被看作对这一思想的路径化扩展。

第三，本文把抽象理解为可复用的压缩结构，这与 **最小描述长度**（MDL）以及 Schmidhuber 的压缩进展理论相呼应。MDL 认为好模型能够缩短对数据的描述；压缩进展理论则把可压缩性的提升视为好奇心和发现的重要驱动力。本文进一步提出：抽象结构之所以被长期保留，是因为它们能在多个情境中降低未来自由能，并且收益超过维护成本。

第四，自由能原理和主动推理为感知、行动、注意力和学习提供了不确定性降低的形式化背景。本文的贡献在于把主动推理、资源理性、控制价值、记忆保留、抽象形成和认知过载统一到一个类最小作用量的认知路径模型中。

---

## 3. 从自由能到认知作用量

在自由能原理和主动推理框架中，系统通过最小化变分自由能来让内部信念更接近外部世界的生成结构。

设环境隐状态为：

$$x_t\in\mathcal{X}$$

观测为：

$$o_t\in\mathcal{O}$$

系统维护一个近似后验：

$$q(x_t)$$

对于某个生成模型 $m^k$，瞬时变分自由能可以写成：

$$F_t^k[q^k]=\mathbb{E}_{q^k}\left[\ln q^k(x_t)-\ln p(o_t,x_t|o_{1:t-1},m^k)\right]$$

它也可以分解为：

$$F_t^k[q^k]=D_{KL}\left[q^k(x_t)\Vert p(x_t|o_{1:t},m^k)\right]-\ln p(o_t|o_{1:t-1},m^k)$$

由于 KL 散度非负，因此：

$$F_t^k[q^k]\ge -\ln p(o_t|o_{1:t-1},m^k)$$

这意味着，最小化变分自由能等价于让系统的内部信念尽量接近贝叶斯后验，同时提高当前观测在模型下的可解释性。

但大脑不是只在某一个瞬间最小化误差。它必须在连续时间中维持感知、记忆、行动和控制。因此，我们需要把瞬时代价扩展为跨时间总代价。

预期自由能通常是面向未来策略的量。为了避免把当前瞬时自由能 $F_t$ 与未来策略评价 $G$ 混在同一个时间下标中，可以先定义一个滚动未来视野上的预期自由能：

$$G_{t:t+H}(\pi_t)=\int_t^{t+H}G_\tau(\pi_t)d\tau$$

再定义局部预期自由能率：

$$g_t(\pi_t)=\frac{1}{H}G_{t:t+H}(\pi_t)$$

这样，积分中的每一项都可以被理解为时刻 $t$ 的局部代价率。

可以定义资源约束下的认知作用量：

$$\mathcal{A}_{cog}[q,\pi]=\int_{t_0}^{t_1}\left[F_t(q_t)+\beta g_t(\pi_t)+\lambda_E E_t+\lambda_A A_t+\lambda_M M_t+\lambda_C C_t\right]dt$$

其中：

- $F_t$ 表示当前变分自由能；
- $g_t(\pi_t)$ 表示滚动未来视野诱导出的局部预期自由能率；
- $E_t$ 表示能量消耗；
- $A_t$ 表示注意力消耗；
- $M_t$ 表示记忆维护和检索成本；
- $C_t$ 表示高阶控制成本；
- $\lambda_E,\lambda_A,\lambda_M,\lambda_C$ 是不同资源代价的权重。

这里的能量项 $E_t$ 不应被理解为一个与自由能无关、后接上去的线性惩罚项。在预测加工解释中，能量消耗的一部分来自神经系统为编码预测误差而产生的放电活动。若模型 $m^k$ 给出预测 $\hat o_t^k$，预测误差为：

$$\varepsilon_t^k=o_t-\hat o_t^k$$

则一个粗粒度的操作化形式可以写成：

$$E_t^k=E_{\mathrm{base}}+\chi\,(\varepsilon_t^k)^\top \Pi_t^k\varepsilon_t^k+E_{\mathrm{plastic}}^k(t)$$

其中 $\Pi_t^k$ 表示精度，$E_{\mathrm{plastic}}^k(t)$ 表示突触或阈值改变带来的短期可塑性成本。等价地，在信息论层面，激活成本可以被看作随惊讶度上升：

$$E_{\mathrm{act}}^k(t)\propto -\ln p(o_t|m^k,o_{1:t-1})$$

因此，当学习把反复出现的模式变成强先验时，$p(o_t|m^k,o_{1:t-1})$ 上升，预测误差下降，激活成本就会逼近较低的基线水平。这样，模型就不会把“自由能降低”和“能量节省”当作两个彼此无关的记账项。

于是，大脑的认知路径选择可以写成：

$$(q^{\ast},\pi^{\ast})=\arg\min_{q,\pi}\mathcal{A}_{cog}[q,\pi]$$

这不是物理作用量的直接等价物，而是资源约束主动推理系统的路径代价泛函。

---

## 4. 大脑不是选择唯一解释，而是维护多个候选路径

真实认知并不是从一开始就得到唯一答案。面对复杂情境时，大脑往往同时激活多个候选解释：

$$H_1,H_2,\ldots,H_K$$

每个候选解释对应一个生成模型 $m^k$，并拥有自己的信念权重：

$$\rho_t^k=p(m^k|o_{1:t})$$

满足：

$$\rho_t^k\ge 0,\quad\sum_{k=1}^{K}\rho_t^k=1$$

整体信念可以表示为混合分布：

$$q(x_t)=\sum_{k=1}^{K}\rho_t^k\,q^k(x_t)$$

如果每条路径都有自己的总代价率：

$$\ell_t^k = F_t^k + \beta g_t^k + \lambda_E E_t^k + \lambda_A A_t^k + \lambda_M M_t^k + \lambda_C C_t^k$$

那么模型权重可以按照指数规则更新：

$$\rho_{t+\Delta t}^k=\frac{\rho_t^k\exp\left(-\ell_t^k \Delta t\right)}{\sum_j\rho_t^j\exp\left(-\ell_t^j \Delta t\right)}$$

当 $\Delta t \to 0$ 时，可以得到复制器动力学：

$$\frac{d\rho_t^k}{dt}=\rho_t^k\left(\bar{\ell}_t-\ell_t^k\right)$$

其中：

$$\bar{\ell}_t=\sum_j \rho_t^j \ell_t^j$$

这说明：

> 低于平均代价的解释路径权重上升，高于平均代价的解释路径权重下降。

这可以解释许多认知现象：

- 犹豫：多个候选路径代价接近；
- 顿悟：某条路径突然获得明显优势；
- 联想：相邻路径被共同激活；
- 疲劳时思维变窄：低成本熟悉路径占优；
- 压力下抽象能力下降：高阶整合路径代价过高，被系统抑制。

因此，大脑的认知状态不是一个固定答案，而是多个候选解释在有限资源约束下的动态竞争。

---

## 5. 神经放电积分提供微观基础

单个神经元可以被粗略描述为一个时间积分器：

$$\tau \frac{dV}{dt}=-(V - V_{\mathrm{rest}}) + I(t)$$

当膜电位达到阈值：

$$V(t)\ge V_{\mathrm{th}}$$

神经元放电。

这说明神经元本身就是一个“输入积分-阈值触发”系统：

$$\text{输入累积}\rightarrow\text{达到阈值}\rightarrow\text{放电}$$

但这个方程只是最简的开环形式。一个会学习的认知回路不能把 $I(t)$ 和 $V_{\mathrm{th}}$ 一直当成静态常数。在预测编码回路中，有效驱动力主要不是原始输入本身，而是预测误差。设底层信号为 $y_t$，高层记忆或先验给出预测：

$$\hat y_t=g(m_t,W_t)$$

误差神经元携带：

$$\varepsilon_t=y_t-\hat y_t$$

于是膜电位动力学可以更明确地写成：

$$\tau \frac{dV}{dt}=-(V-V_{\mathrm{rest}})+W_t^\top\varepsilon_t+I_{\mathrm{value}}(t)+\zeta_t$$

初次暴露时，$m_t$ 和 $W_t$ 还不能很好预测输入，因此 $\varepsilon_t$ 很大，放电强、能耗高。多次暴露后，自上而下的预测会抵消相当一部分自下而上的驱动。如果 $\hat y_t\approx y_t$，则 $\varepsilon_t\approx 0$，回路只需要接近基线水平的活动。

学习还会改变未来激活这个回路所需的初始条件。最小的突触可塑性方程可以写成：

$$\frac{dW_{ij}}{dt}=\eta_W\,\mathrm{pre}_j(t)\,\mathrm{post}_i(t)-\lambda_W W_{ij}$$

或者，在误差校正的预测编码形式中写成：

$$\frac{dW}{dt}=\eta_W\,\varepsilon_t\,\phi(m_t)^\top-\lambda_W W$$

阈值本身也应是动态的。一个稳态内在可塑性规则可以写成：

$$\tau_{\mathrm{th}}\frac{dV_{\mathrm{th}}}{dt}=\kappa_{\mathrm{th}}\left(r_t-r_{\mathrm{target}}\right)$$

其中 $r_t$ 表示近期放电率。持续过度放电会提高阈值，低于目标放电率则会降低阈值。这样，回路就获得了一个物理机制：已经被先验解释的日常输入被过滤掉，只有真正违背先验、具有信息量的偏差才更容易触发放电。

推广到认知层面，某个信念、记忆或神经回路的激活强度 $b$ 更严格地说应被看作一个粗粒度的现象学模型：

$$\tau \dot b=-\alpha b-\Gamma_b\frac{\partial F}{\partial b}+\gamma V_{\mathrm{value}}-\eta E+\xi_t$$

其中：

- $b$ 表示某个信念或回路的激活强度；
- $-\partial F/\partial b$ 表示作用在该激活变量上的自由能梯度；
- $V_{\mathrm{value}}$ 表示价值信号；
- $E$ 表示能量或代价项；
- $\Gamma_b,\alpha,\gamma,\eta$ 是调节参数；
- $\xi_t$ 表示未显式建模的噪声或波动。

由于在预测加工模型中，预测误差通常是自由能梯度的主要组成部分，上式可以启发式地近似写成：

$$\tau \frac{db}{dt}\approx-\alpha b+\beta \varepsilon+\gamma V_{\mathrm{value}}-\eta E$$

这个近似式应被理解为现象学摘要，而不是从自由能原理严格推出的动力学方程。更闭环的写法是：

$$\tau \frac{db}{dt}\approx-\alpha b+\beta \varepsilon(m,W)+\gamma V_{\mathrm{value}}-\eta E_{\mathrm{act}}(\varepsilon)$$

其中：

$$E_{\mathrm{act}}(\varepsilon)=E_{\mathrm{base}}+\chi\,\varepsilon^\top\Pi\varepsilon$$

这意味着：

> 神经系统通过对预测误差、价值、可塑性、阈值和能量代价进行时间积分，决定哪些回路被激活、强化、抑制或遗忘。

因此，神经放电积分可以被看作自由能最小化和路径选择的微观动力学基础。关键在于，记忆和先验形成并不是外加在这个动力学之后的东西；它们正是重复误差改变 $W_t$ 和 $V_{\mathrm{th}}(t)$ 的闭环过程，使未来处理相似输入所需的放电量下降。

---

## 6. 记忆不是录像，而是低代价路径的保存

大脑不会每次都从零开始计算。它会保存过去有用的路径，使未来处理相似情境时更加省力。

因此，记忆不是对过去的完整复制，而是对过去路径的压缩、筛选和重构。

设记忆痕迹强度为：

$$m_i(t)\in[0,1]$$

它的动力学可以写成：

$$dm_i=\left[-\lambda_i m_i+\alpha r_i(t)+\beta_m \max\left(0,\Delta F_i(t)\right)\right]dt+\sigma_m\,dW_t^{i}$$

其中：

$$\Delta F_i(t)=F_{\text{baseline}}(t)-F_{\text{with }m_i}(t)$$

表示该记忆痕迹带来的自由能降低量。

如果 $\Delta F_i(t)>0$，说明该记忆帮助系统更好地解释当前情境；如果 $\Delta F_i(t)\le 0$，说明它没有带来解释收益，甚至可能造成干扰。

如果这个记忆痕迹要真正成为先验，它还必须改变未来处理同类输入的回路。这个耦合关系可以写成：

$$p(o_t|m_i)\uparrow\quad\Longleftrightarrow\quad \varepsilon_t(o_t,m_i)\downarrow\quad\Longleftrightarrow\quad E_{\mathrm{act}}(o_t|m_i)\downarrow$$

也就是说：

$$E_{\mathrm{act}}(o_t|m_i)=E_{\mathrm{base}}+\chi\left[-\ln p(o_t|m_i)\right]$$

或者在高斯预测编码近似下：

$$E_{\mathrm{act}}(o_t|m_i)=E_{\mathrm{base}}+\chi\,\varepsilon_t^\top\Pi_t\varepsilon_t$$

因此，记忆痕迹具有两个绑定在一起的效果：它通过改善预测降低未来自由能，同时通过减少需要放电传递的误差信号降低未来代谢成本。

其中 $\max(0,\Delta F_i(t))$ 是局部、瞬时的巩固信号。长期记忆保留规则可以被理解为这些局部收益所诱导出的价值函数。定义瞬时记忆收益：

$$r_i^{mem}(t)=\max(0,\Delta F_i(t))-c_i(m_i,t)$$

其中 $c_i(m_i,t)$ 表示该记忆痕迹的持续维护成本。于是保留该记忆的长期价值可以写成：

$$V_i(t)=\mathbb{E}\left[\int_t^\infty e^{-\gamma(s-t)}r_i^{mem}(s)ds\right]$$

因此，记忆保留的原则可以写成：

$$\text{keep }m_i\quad\Longleftrightarrow\quad V_i(t)>0$$

也就是：

$$\mathbb{E}\left[\int_t^\infty e^{-\gamma(s-t)}\,\Delta F_i(s)\,ds\right]>C_{\text{maintain}}(m_i)$$

这意味着：

> 长期保留的不是最完整的细节，而是那些能够反复降低未来自由能、且维护成本低于收益的结构。

这也解释了抽象为什么会出现。

抽象并不是额外附加的高级能力，而是记忆动力学在有限资源下的自然结果。当多个经验片段共享某种结构，并且该结构能在不同情境中反复降低自由能时，它就更容易被保留下来。

可以定义抽象结构 $a$ 的价值：

$$V(a)=\sum_j w_j \Delta F_a(e_j)-C_{\text{maintain}}(a)$$

当：

$$V(a)>0$$

抽象结构就值得被保留。

所以：

> 抽象是跨多个经验都能降低自由能、且维护成本低于总收益的压缩结构。

---

## 7. 大脑不是追求眼前最省力，而是追求长期低代价

一种常见误解是：大脑总是选择最简单、最省力的办法。

这个说法只对了一半。

更准确地说：

> 大脑倾向于形成那些在长期上具有较低累计自由能的回路；这些回路不一定局部最简单，但必须在预测误差、能量消耗、记忆维护成本和行动收益之间达到稳定折中。

很多高级能力在形成初期都非常耗能：

- 学数学初期很费力；
- 学编程初期很费力；
- 学语言初期很费力；
- 建立抽象模型初期很费力。

但一旦稳定回路形成，未来处理同类问题时就会显著降低认知成本。

原因并不只是系统保存了一个符号化答案，而是学习后的回路改变了未来输入到预测误差的映射：

$$\varepsilon_C(t|m_C)=o_t-g_C(m_C,W_C)$$

因此也改变了未来激活成本：

$$E_C(t|m_C)=E_{\mathrm{base}}+\chi\,\varepsilon_C(t|m_C)^\top\Pi_C\varepsilon_C(t|m_C)$$

当 $m_C$ 和 $W_C$ 成为某类情境的更好先验时，$\varepsilon_C$ 会下降。长期路径之所以更省，是因为回路不再需要从零重新生成同一个推断。

因此，大脑不是单纯选择：

$$\text{当前最省力路径}$$

而是选择：

$$\text{长期累计代价较低的路径}$$

也就是：

$$C^{\ast}=\arg\min_{C}\int_{t_0}^{t_1}\left[F_C(t)+\lambda_E E_C(t)+\lambda_M M_C(t)+\lambda_C C_C(t)\right]dt$$

---

## 8. 过载、疲劳与注意力塌缩

设系统当前总代价率为：

$$L(t)=F_t+\beta g_t+\lambda_E E_t+\lambda_A A_t+\lambda_M M_t+\lambda_C C_t$$

资源边界为：

$$R_{\text{total}}(t)$$

当：

$$L(t)<R_{\text{total}}(t)$$

系统可以维持稳定推理、记忆整合和行动控制。

但当：

$$L(t)\ge R_{\text{total}}(t)$$

系统进入过载状态。

此时，大脑可能采取保护性策略：

- 降低输入增益；
- 缩窄注意力范围；
- 退回熟悉回路；
- 降低抽象推理能力；
- 减少高阶整合；
- 表现出疲劳、脑雾、眩晕或回避反应。

可以引入有效温度：

$$T_{\text{eff}}(t)=T_0 \max\left(1,\dfrac{L(t)}{R_{\text{total}}(t)}\right)$$

当负载越接近或超过资源边界时，系统噪声增强，信念更新变得不稳定：

$$d\mu_t^k=-\Gamma_\mu\nabla_\mu F_t^k \, dt+\sqrt{2\Gamma_\mu T_{\text{eff}}(t)}\,dW_t^{\mu,k}$$

这可以作为认知过载的形式化模型。

不过需要谨慎：眩晕、疲劳和脑雾在真实生理中可能涉及前庭系统、血压、自主神经、代谢状态、炎症和睡眠等多种因素。因此，该模型不能被理解为对这些现象的唯一解释，而应被视为一种可检验的认知层解释假说。

---

## 9. 高阶观察者不是小人，而是元控制系统

在这个框架中，“高阶观察者”不应被理解为大脑里还有一个小人在观看一切。

更严格地说，它是一组调节低层推断过程的慢变量元控制系统。

可以定义元控制变量：

$$\theta_t=\{\lambda_E,\lambda_A,\lambda_M,\lambda_C,\Gamma_\mu,T_{\text{eff}},\kappa\}$$

它们调节：

- 不同资源代价的权重；
- 信念更新速度；
- 注意力精度；
- 探索噪声；
- 候选路径竞争温度；
- 高阶控制增益。

元控制目标可以写成：

$$J(\theta)=\mathbb{E}\left[\int_0^T \ell_t(\theta)\,dt\right]$$

元学习动力学为：

$$d\theta_t=-\eta_\theta\nabla_\theta J(\theta_t)\,dt+\sigma_\theta\,dW_t^{\theta}$$

因此，高阶观察者的严格版本是：

> 一个调节精度、成本权重、更新速度和有效温度的元控制系统。

它的功能不是神秘地“看见意识”，而是在多条候选认知路径之间重新分配有限资源，使系统尽量维持在长期低代价路径上。

---

## 10. 统一模型

综合以上部分，可以得到一个资源约束主动推理系统：

$$\begin{aligned}
d\mu_t^k &= -\Gamma_\mu\nabla_\mu F_t^k\,dt + \sqrt{2\Gamma_\mu T_{\text{eff}}(t)}\,dW_t^{\mu,k} \\
dz_t^k &= -\ell_t^k\,dt + \sigma_\rho\,dW_t^{z,k} \\
\rho_t^k &= \frac{e^{z_t^k}}{\sum_j e^{z_t^j}} \\
dm_i &= \left[-\lambda_i m_i + \alpha r_i(t) + \beta_m\max\left(0,\Delta F_i(t)\right)\right]dt + \sigma_m\,dW_t^{m,i} \\
d\theta_t &= -\eta_\theta\nabla_\theta J(\theta_t)\,dt + \sigma_\theta\,dW_t^{\theta}
\end{aligned}$$

其中：

$$\begin{aligned}
\varepsilon_t^k &= o_t-g^k(m_t,W_t^k) \\
E_t^k &= E_{\mathrm{base}}+\chi(\varepsilon_t^k)^\top\Pi_t^k\varepsilon_t^k+E_{\mathrm{plastic}}^k(t) \\
\ell_t^k &= F_t^k + \beta g_t^k + \lambda_E E_t^k + \lambda_A A_t^k + \lambda_M M_t^k + \lambda_C C_t^k
\end{aligned}$$

使记忆真正成为先验的微观变量可以追加为：

$$\begin{aligned}
dW_t^k &= \left[\eta_W\varepsilon_t^k\phi(m_t)^\top-\lambda_W W_t^k\right]dt+\sigma_W dW_t^{W,k} \\
\tau_{\mathrm{th}}\,dV_{\mathrm{th},t}^k &= \kappa_{\mathrm{th}}\left(r_t^k-r_{\mathrm{target}}\right)dt
\end{aligned}$$

这套模型把以下闭环机制统一到一个框架中：

$$\text{有限资源}\rightarrow\text{神经积分}\rightarrow\text{预测误差}\rightarrow\text{可塑性与动态阈值}\rightarrow\text{先验}\rightarrow\text{未来误差和能耗降低}\rightarrow\text{认知作用量}\rightarrow\text{多路径竞争}\rightarrow\text{记忆动力学}\rightarrow\text{抽象}\rightarrow\text{元控制}\rightarrow\text{适应性智能}$$

---

## 11. 潜在实验预测与可操作化

这个模型只有在能够连接到可观测量时，才具有科学价值。$\lambda_A,\lambda_C,\lambda_M$ 和 $T_{\mathrm{eff}}$ 不应被理解为可以直接读取的神经量，而应被理解为通过行为和生理数据拟合出来的潜变量。

可以提出几类可检验预测。

第一，在双任务负荷、时间压力、感官噪声或睡眠不足条件下，模型预测有效温度 $T_{\mathrm{eff}}$ 上升。候选路径选择会变得更随机，选择分布的熵会升高：

$$P(k)\propto \exp\left(-\frac{\mathcal{A}_k}{T_{\mathrm{eff}}}\right)$$

随着 $T_{\mathrm{eff}}$ 上升，选择分布应变平，反应时变异性应增加，系统也更容易依赖熟悉的低成本策略。

第二，当控制成本上升时，即使高控制策略更准确，系统也会减少对它的分配。这预测了更大的任务切换成本、更强的 Stroop 式干扰、更弱的工作记忆维持和更多习惯性反应。可用的经验代理指标包括反应时、错误率、主观努力评分、瞳孔直径、额中线 theta，以及 dACC/PFC 的 BOLD 活动。

第三，记忆价值规则预测：那些能降低未来自由能的记忆痕迹更容易被保留和重激活。这可以通过延迟回忆、再认、睡眠后巩固、表征重激活和迁移任务来检验。若某个痕迹能改善之后的预测或行动，它应比同样生动但不能降低未来不确定性的痕迹更容易保留。

第四，抽象规则预测：多个任务之间共享结构越强，系统越倾向形成压缩表征。在资源压力下，抽象深度应下降，被试会更多依赖具体熟悉样例，而较少调用高阶规则。在计算模型中，这应表现为从高层潜变量向低层缓存策略的转移。

这些预测不能证明真实大脑精确最小化 $\mathcal{A}_{cog}$，但它们让模型具有可证伪性：如果负荷、控制成本或记忆价值的操纵没有按照预测方向改变选择熵、控制分配、记忆保留或抽象水平，模型就需要被修正。

---

## 12. 哪些可以严格证明，哪些仍是假说？

这个框架中有一部分是严格的数学结果：

| 命题 | 状态 |
|---|---|
| 变分自由能上界负对数证据 | 可证明 |
| 最小化自由能等价于逼近贝叶斯后验 | 可证明 |
| 资源约束推出拉格朗日路径泛函 | 可证明 |
| 权重 softmax 来自累计代价 | 可证明 |
| 连续极限给出复制器方程 | 可证明 |
| logits 保证权重合法 | 可证明 |
| 梯度流使自由能单调不增 | 在给定条件下可证明 |
| 记忆保留阈值规则 | 在净收益最大化假设下可证明 |
| 抽象结构保留条件 | 可由记忆规则推广得到 |

但也有一些部分不能被直接证明，只能作为可检验假说：

| 主张 | 状态 |
|---|---|
| 真实大脑精确最小化该认知作用量 | 假说 |
| 能量、注意力、记忆和控制成本的具体形式唯一正确 | 假说 |
| 高阶元控制等同于某个具体脑区功能 | 需要实验支持 |
| 有效温度精确表示认知过载 | 有效模型 |
| 眩晕完全由认知负载超过资源边界导致 | 不能这样断言 |
| 记忆系统精确计算未来自由能收益 | 可能只是近似启发式 |
| 抽象一定等于低自由能压缩结构 | 强理论假说 |

因此，本文的定位不是“证明大脑就是这样”，而是提出：

> 一个可计算、可模拟、可产生预测的规范性认知模型。

---

## 13. 最终命题

可以把整个理论浓缩为一句话：

> 大脑并不是在无限计算中寻找真理，而是在有限资源下寻找一条低作用量的认知路径；神经放电积分提供微观动力学，自由能最小化提供误差修正原则，记忆动力学保存可复用的低代价回路，而高阶观察者作为元控制系统，在多条候选路径之间分配资源。当认知负载超过系统边界时，低作用量路径难以维持，系统便可能表现为疲劳、注意力塌缩、高阶整合失败和认知不稳定。

更学术化地说：

> 大脑可以被理解为一种类最小作用量的认知系统：在有限能量、有限注意力、有限时间和有限记忆容量下，神经系统倾向于选择那些能够最小化累计变分自由能、同时维持自我-环境适应性耦合的感知、记忆与行动路径。

这就是“类最小作用量认知模型”的核心。

---

## 参考文献

Friston, K. (2010). The free-energy principle: a unified brain theory? *Nature Reviews Neuroscience*, 11, 127-138.

Lieder, F., & Griffiths, T. L. (2020). Resource-rational analysis: Understanding human cognition as the optimal use of limited computational resources. *Behavioral and Brain Sciences*, 43, e1.

Parr, T., Pezzulo, G., & Friston, K. J. (2022). *Active Inference: The Free Energy Principle in Mind, Brain, and Behavior*. MIT Press.

Rissanen, J. (1978). Modeling by shortest data description. *Automatica*, 14(5), 465-471.

Schmidhuber, J. (2009). Driven by compression progress: A simple principle explains essential aspects of subjective beauty, novelty, surprise, interestingness, attention, curiosity, creativity, art, science, music, jokes. *Dagstuhl Seminar Proceedings*.

Shenhav, A., Botvinick, M. M., & Cohen, J. D. (2013). The expected value of control: An integrative theory of anterior cingulate cortex function. *Neuron*, 79(2), 217-240.
