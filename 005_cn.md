# 005｜资源约束下的认知路径优化：从局部可塑性到低代价预测推理

**作者：** zq

---

## 摘要

本文提出一种资源约束下的认知路径优化框架：大脑并不是在无限计算中寻找绝对真理，而是在有限能量、有限注意力、有限时间和有限记忆容量下，进入一条能够稳定降低未来不确定性、支持行动并维持自我-环境耦合的低代价路径。

本文的正式数学对象不是哈密顿意义上的物理作用量泛函，而是耗散认知动力学中的资源约束路径代价泛函。在该框架中，预测编码提供误差抵消机制，神经放电积分提供微观动力学基础，突触可塑性和动态阈值把重复刺激转化为先验，变分自由能提供观察者层面的代价函数，资源约束形成跨时间路径代价，多模型贝叶斯竞争解释候选解释之间的动态选择，记忆动力学通过局部可塑性保存可复用的低代价路径，而高阶观察者则被形式化为调节成本权重、精度、更新增益和有效温度的元控制系统。

---

## 1. 问题的出发点

经典物理中的最小作用量原理告诉我们：系统的真实演化路径并不是任意发生的，而是在某种总体约束下，使作用量达到驻定或最优的路径。本文只借用其中“路径可以由累计代价进行比较”的宽泛直觉。

如果将这个思想迁移到认知系统中，我们可以提出一个类似但不等同的命题：

> 大脑不是无限计算系统，而是在有限资源下选择一条足够稳定、足够低成本、足够能解释世界并支持行动的认知路径。

本文的核心对象不是物理中的动能减势能积分，而是认知系统在时间中累计付出的路径代价。它包括预测误差、神经能耗、注意力负担、记忆维护成本、高阶控制成本和行动成本。

因此，大脑可以被理解为一种**资源约束下的路径优化系统**。更准确地说：

> 大脑的智能不是无限计算，而是有限资源下的路径选择。

---

## 2. 与既有理论的关系

本文并不是要替代已有的有限理性、认知控制、压缩学习或主动推理理论，而是试图用一个统一的路径代价泛函把它们连接起来。

第一，本文的有限资源假设与 **resource-rational analysis** 高度相关。Lieder 和 Griffiths 的资源理性分析把人类认知理解为有限计算资源下对认知操作的最优使用。本文继承这一精神，但把时间、注意力、记忆和控制成本嵌入主动推理的跨时间路径泛函中。

第二，本文对高阶控制成本的处理与 Shenhav、Botvinick 和 Cohen 提出的 **Expected Value of Control** 理论高度一致。EVC 理论认为，控制投入取决于预期收益、所需控制强度和认知努力成本。本文中的 $\lambda_C C_t$ 与元控制变量 $\theta_t$ 可以被看作对这一思想的路径化扩展。

第三，本文把抽象理解为可复用的压缩结构，这与 **最小描述长度**（MDL）以及 Schmidhuber 的压缩进展理论相呼应。MDL 认为好模型能够缩短对数据的描述；压缩进展理论则把可压缩性的提升视为好奇心和发现的重要驱动力。本文进一步提出：抽象结构之所以会长期留下来，是因为重复共激活和巩固留下了可复用回路；从观察者层面看，这些回路能在多个情境中降低未来自由能，并且收益超过维护成本。

第四，自由能原理和主动推理为感知、行动、注意力和学习提供了不确定性降低的形式化背景。本文的贡献在于把主动推理、资源理性、控制价值、记忆保留、抽象形成和认知过载统一到一个资源约束下的认知路径优化模型中。

---

## 3. 从自由能到认知路径代价

在自由能原理和主动推理框架中，系统可以被建模为通过最小化变分自由能来让内部信念更接近外部世界的生成结构。这是宏观层面和规范层面的描述，并不意味着单个神经元显式计算自由能泛函，也不意味着神经细胞会比较未来价值。

设环境隐状态为：

$$x_t\in\mathcal{X}$$

观测为：

$$o_t\in\mathcal{O}$$

系统维护一个近似后验：

$$q(x_t)$$

对于某个生成模型 $m^k$，瞬时变分自由能可以写成：

$$F_t^k[q^k]=\mathbb{E}_{q^k}\left[\ln q^k(x_t)-\ln p(o_t,x_t|o_{1:t-1},m^k)\right]$$

它也可以分解为：

$$F_t^k[q^k]=D_{KL}\left[q^k(x_t)\Vert p(x_t|o_{1:t},m^k)\right]-\ln p(o_t|o_{1:t-1},m^k)$$

由于 KL 散度非负，因此：

$$F_t^k[q^k]\ge -\ln p(o_t|o_{1:t-1},m^k)$$

这意味着，最小化变分自由能等价于让系统的内部信念尽量接近贝叶斯后验，同时提高当前观测在模型下的可解释性。在生物实现层面，与之对应的机制可以只是局部误差信号、重复激活、突触可塑性、阈值适应和神经调质性巩固。自由能是我们用来描述这些局部过程总体效果的建模语言。

但大脑不是只在某一个瞬间最小化误差。它必须在连续时间中维持感知、记忆、行动和控制。因此，我们需要把瞬时代价扩展为跨时间总代价。

预期自由能通常是面向未来策略的量。为了避免把当前瞬时自由能 $F_t$ 与未来策略评价 $G$ 混在同一个时间下标中，可以先定义一个滚动未来视野上的预期自由能：

$$G_{t:t+H}(\pi_t)=\int_t^{t+H}G_\tau(\pi_t)d\tau$$

再定义局部预期自由能率：

$$\mathcal{G}_t(\pi_t)=\frac{1}{H}G_{t:t+H}(\pi_t)$$

这样，积分中的每一项都可以被理解为时刻 $t$ 的局部代价率。

可以定义资源约束下的认知路径代价泛函：

$$\mathcal{C}_{path}[q,\pi]=\int_{t_0}^{t_1}\left[F_t(q_t)+\beta \mathcal{G}_t(\pi_t)+\lambda_E E_t+\lambda_A A_t+\lambda_M M_t+\lambda_C C_t\right]dt$$

其中：

- $F_t$ 表示当前变分自由能；
- $\mathcal{G}_t(\pi_t)$ 表示滚动未来视野诱导出的局部预期自由能率；
- $E_t$ 表示能量消耗；
- $A_t$ 表示注意力消耗；
- $M_t$ 表示记忆维护和检索成本；
- $C_t$ 表示高阶控制成本；
- $\lambda_E,\lambda_A,\lambda_M,\lambda_C$ 是不同资源代价的权重。

这里的能量项 $E_t$ 不应被理解为一个与自由能无关、后接上去的线性惩罚项。在预测加工解释中，能量消耗的一部分来自神经系统为编码预测误差而产生的放电活动。若模型 $m^k$ 给出预测 $\hat o_t^k$，预测误差为：

$$\varepsilon_t^k=o_t-\hat o_t^k$$

则一个粗粒度的操作化形式可以写成：

$$E_t^k=E_{\mathrm{base}}+\chi\,(\varepsilon_t^k)^\top \Pi_t^k\varepsilon_t^k+E_{\mathrm{plastic}}^k(t)$$

其中 $\Pi_t^k$ 表示精度，$E_{\mathrm{plastic}}^k(t)$ 表示突触或阈值改变带来的短期可塑性成本。等价地，在信息论层面，激活成本可以被看作随惊讶度上升：

$$E_{\mathrm{act}}^k(t)\propto -\ln p(o_t|m^k,o_{1:t-1})$$

因此，当重复刺激留下更强的回路痕迹时，建模者可以把同一过程描述为 $p(o_t|m^k,o_{1:t-1})$ 上升、预测误差下降、激活成本逼近较低的基线水平。这样，模型就不会把“自由能降低”和“能量节省”当作两个彼此无关的记账项，同时也保留了局部神经适应和观察者层面贝叶斯描述之间的区别。

于是，大脑的认知路径选择可以写成：

$$(q^{\ast},\pi^{\ast})=\arg\min_{q,\pi}\mathcal{C}_{path}[q,\pi]$$

这不是物理作用量的直接等价物，而是资源约束主动推理系统的路径代价泛函。

---

## 4. 大脑不是选择唯一解释，而是维护多个候选路径

真实认知并不是从一开始就得到唯一答案。面对复杂情境时，大脑往往同时激活多个候选解释：

$$H_1,H_2,\ldots,H_K$$

每个候选解释对应一个生成模型 $m^k$，并拥有自己的信念权重：

$$\rho_t^k=p(m^k|o_{1:t})$$

满足：

$$\rho_t^k\ge 0,\quad\sum_{k=1}^{K}\rho_t^k=1$$

整体信念可以表示为混合分布：

$$q(x_t)=\sum_{k=1}^{K}\rho_t^k\,q^k(x_t)$$

如果每条路径都有自己的总代价率：

$$\ell_t^k = F_t^k + \beta \mathcal{G}_t^k + \lambda_E E_t^k + \lambda_A A_t^k + \lambda_M M_t^k + \lambda_C C_t^k$$

那么模型权重可以按照指数规则更新：

$$\rho_{t+\Delta t}^k=\frac{\rho_t^k\exp\left(-\ell_t^k \Delta t\right)}{\sum_j\rho_t^j\exp\left(-\ell_t^j \Delta t\right)}$$

当 $\Delta t \to 0$ 时，可以得到复制器动力学：

$$\frac{d\rho_t^k}{dt}=\rho_t^k\left(\bar{\ell}_t-\ell_t^k\right)$$

其中：

$$\bar{\ell}_t=\sum_j \rho_t^j \ell_t^j$$

这说明：

> 低于平均代价的解释路径权重上升，高于平均代价的解释路径权重下降。

这可以解释许多认知现象：

- 犹豫：多个候选路径代价接近；
- 顿悟：某条路径突然获得明显优势；
- 联想：相邻路径被共同激活；
- 疲劳时思维变窄：低成本熟悉路径占优；
- 压力下抽象能力下降：高阶整合路径代价过高，被系统抑制。

因此，大脑的认知状态不是一个固定答案，而是多个候选解释在有限资源约束下的动态竞争。

---

## 5. 神经放电积分提供微观基础

单个神经元可以被粗略描述为一个时间积分器：

$$\tau \frac{dV}{dt}=-(V - V_{\mathrm{rest}}) + I(t)$$

当膜电位达到阈值：

$$V(t)\ge V_{\mathrm{th}}$$

神经元放电。

这说明神经元本身就是一个“输入积分-阈值触发”系统：

$$\text{输入累积}\rightarrow\text{达到阈值}\rightarrow\text{放电}$$

但这个方程不能被过度解读。LIF 动力学可以作为记忆动力学的微观锚点，但它本身不能推出记忆、先验、价值、多巴胺、自由能、抽象或元控制。要从神经放电方程接到记忆动力学，必须额外加入几类跨层级假设：误差编码假设、可塑性规则、稳态调节规则、价值门控规则，以及从神经群活动到记忆变量的粗粒化映射规则。

### From LIF to Memory Dynamics: Conditional Derivation（从 LIF 到记忆动力学的条件推导）

条件推导的第一步，是把原始输入驱动和预测误差驱动区分开。在预测编码回路中，有效驱动力不只是自下而上的裸输入，而是输入与生成模型预测之间的不匹配。设底层信号为 $y_t$，高层记忆或先验给出预测：

$$\hat y_t=G(m_t,W_t)$$

误差神经元携带：

$$\varepsilon_t=y_t-\hat y_t$$

但这不应被理解为每个神经元都直接接收完整的 $\varepsilon_t$ 向量。更严谨的近似是区分误差神经元和状态/信念神经元：

$$\tau_\varepsilon \dot V_\varepsilon
=-(V_\varepsilon-V_{\mathrm{rest}})
+A\left(y_t-G(m_t,W_t)\right)+\zeta_\varepsilon$$

$$\tau_m \dot V_m
=-(V_m-V_{\mathrm{rest}})
+B\Pi_t\varepsilon_t+I_{\mathrm{value}}(t)+\zeta_m$$

其中 $\Pi_t$ 表示精度。真正驱动信念更新的通常不是裸误差，而是精度加权预测误差。初次暴露时，$m_t$ 和 $W_t$ 还不能很好预测输入，因此 $\varepsilon_t$ 很大，误差神经元活动、控制负荷和可塑性压力都较高。多次暴露后，可预测部分被自上而下模型解释，误差信号和控制负荷下降。这里不能说所有神经活动都会下降；更准确地说，是单位任务所需的预测误差处理、努力控制和可塑性更新成本通常下降，而表征、运动或习惯回路可能仍然高效活动。

学习还会改变未来激活这个回路所需的初始条件。最小的突触可塑性方程可以写成：

$$\frac{dW_{ij}}{dt}=\eta_W\,\mathrm{pre}_j(t)\,\mathrm{post}_i(t)-\lambda_W W_{ij}$$

这应被理解为 rate-based Hebbian 近似。若要更接近脉冲神经元，则可以替换成 STDP 规则，使突触变化的方向和大小依赖前突触与后突触放电的时间差。在误差校正的预测编码形式中，若 $\hat y=W\phi(m)$ 且

$$F_t=\frac{1}{2}\varepsilon_t^\top\Pi_t\varepsilon_t+\frac{1}{2}\lambda_W\lVert W\rVert^2$$

则对 $F_t$ 做梯度下降得到局部更新：

$$\dot W=\eta_W\,\Pi_t\varepsilon_t\,\phi(m_t)^\top-\eta_W\lambda_W W$$

但并不是所有误差都值得学习。可以加入一个由价值和显著性调节的可塑性门控：

$$q_t=\sigma\left(a_\varepsilon\lVert\Pi_t\varepsilon_t\rVert+a_VV_{\mathrm{value},t}+a_N\mathrm{Novelty}_t-a_EE_{\mathrm{plastic},t}\right)$$

于是：

$$\dot W=\eta_W q_t\,\Pi_t\varepsilon_t\,\phi(m_t)^\top-\lambda_W W$$

这个 $q_t$ 是神经调质系统对学习率和巩固窗口的紧凑建模，不应被简单等同为多巴胺本身。

阈值本身也应是动态的。一个稳态内在可塑性规则可以写成：

$$\tau_{\mathrm{th}}\frac{dV_{\mathrm{th}}}{dt}=\kappa_{\mathrm{th}}\left(r_t-r_{\mathrm{target}}\right)$$

其中 $r_t$ 表示近期放电率。持续过度放电会提高阈值，低于目标放电率则会降低阈值。这条规则不是“过滤已预测输入”的主要机制；过滤主要来自 $\varepsilon=y-\hat y$ 的预测误差编码。动态阈值和稳态可塑性的作用，是防止回路因 Hebbian 增强而无限兴奋，或因长期低输入而沉默。也就是说，Hebbian 可塑性让有用通路更容易被招募，稳态可塑性让这种增强保持在稳定范围内。

推广到认知层面，某个信念、记忆或神经回路的激活强度 $b$ 更严格地说应被看作一个粗粒度的现象学模型：

$$\tau \dot b=-\alpha b-\Gamma_b\frac{\partial F}{\partial b}+\gamma V_{\mathrm{value}}-\eta E+\xi_t$$

其中：

- $b$ 表示某个信念或回路的激活强度；
- $-\partial F/\partial b$ 表示作用在该激活变量上的自由能梯度；
- $V_{\mathrm{value}}$ 表示价值信号；
- $E$ 表示能量或代价项；
- $\Gamma_b,\alpha,\gamma,\eta$ 是调节参数；
- $\xi_t$ 表示未显式建模的噪声或波动。

由于在预测加工模型中，预测误差通常是自由能梯度的主要组成部分，上式可以启发式地近似写成：

$$\tau_b \dot b\approx-\alpha b+\beta \Pi_t\varepsilon_t+\gamma V_{\mathrm{value}}-\eta E_{\mathrm{act}}+\xi_t$$

这个近似式应被理解为现象学摘要，而不是从自由能原理严格推出的动力学方程。更闭环的写法是：

$$\tau_b \dot b\approx-\alpha b+\beta \Pi_t\varepsilon(m,W)+\gamma V_{\mathrm{value}}-\eta E_{\mathrm{act}}(\varepsilon)+\xi_t$$

其中：

$$E_{\mathrm{act}}(\varepsilon)=E_{\mathrm{base}}+\chi\,\varepsilon^\top\Pi\varepsilon$$

由于 $F_t$ 中本来就可能包含同样的误差二次型，$E_{\mathrm{act}}$ 不应被理解为对同一个推理目标的重复计数，而应被理解为代谢成本、控制成本或可塑性成本的模型化表示。

记忆变量本身则是神经群层面的粗粒化对象：

$$m_i(t)=\mathcal C_i\left(\{V_j(t),s_j(t),W_{jk}(t),V_{\mathrm{th},j}(t)\}_{j,k\in\Omega_i}\right)$$

在这个粗粒化下，重复经验形成闭环：

$$m_t,W_t\rightarrow \hat y_t\rightarrow \varepsilon_t\rightarrow \Delta W_t,\Delta m_t\rightarrow m_{t+1},W_{t+1}$$

这意味着：

> 神经系统通过对预测误差、价值、可塑性、阈值和能量代价进行时间积分，决定哪些回路被激活、强化、抑制或遗忘。

因此，神经放电积分只能在这个条件意义上被看作自由能最小化和路径选择的微观动力学基础。LIF 提供积分-阈值骨架；预测编码定义什么是有效误差；可塑性改变未来预测；稳态调节维持回路稳定；价值门控决定哪些误差值得巩固；粗粒化映射把神经群活动转化为记忆变量。关键在于，记忆和先验形成并不是外加在这个动力学之后的东西；它们正是重复误差改变 $W_t$、$m_t$、兴奋性和阈值的闭环过程，使未来处理相似输入时的预测误差和处理代价下降。

---

## 6. 记忆不是录像，而是低代价路径的保存

大脑不会每次都从零开始计算。它会保存过去有用的路径，使未来处理相似情境时更加省力。

因此，记忆不是对过去的完整复制，而是对过去路径的压缩、筛选和重构。

设记忆痕迹强度为：

$$m_i(t)\in[0,1]$$

在神经实现层面，它的动力学应写成局部变量，而不是写成神经元显式计算未来自由能收益。一个最小的局部规则是：

$$dm_i=\left[-\lambda_i m_i+\alpha_a a_i(t)+\alpha_c\,\mathrm{coact}_i(t)+\alpha_n n_i(t)\right]dt+\sigma_m\,dW_t^{i}$$

其中：

- $a_i(t)$ 表示该痕迹的近期激活；
- $\mathrm{coact}_i(t)$ 表示局部前后神经元或回路层面的共激活；
- $n_i(t)$ 表示神经调质或巩固信号，例如显著性、奖励、唤醒、睡眠重放或可塑性生物窗口；
- $\lambda_i m_i$ 表示未使用痕迹的衰减或竞争。

同一个过程可以在观察者层面定义为：

$$\Delta F_i^{obs}(t)=F_{\text{baseline}}(t)-F_{\text{with }m_i}(t)$$

这不是神经元计算的量，而是外部建模者归因于该记忆痕迹的自由能降低量。如果 $\Delta F_i^{obs}(t)>0$，说明从模型角度看该记忆帮助系统更好地解释当前情境；如果 $\Delta F_i^{obs}(t)\le 0$，说明它没有观察者层面的解释收益，甚至可能造成干扰。

如果这个记忆痕迹要真正成为先验，它还必须改变未来处理同类输入的回路。这个耦合关系可以写成：

$$\text{重复共激活}\uparrow\quad\Longrightarrow\quad W_i\uparrow,\ V_{\mathrm{th},i}\ \text{适应}\quad\Longrightarrow\quad \varepsilon_t(o_t,m_i)\downarrow,\ E_{\mathrm{act}}(o_t|m_i)\downarrow$$

也就是说，在观察者层面：

$$E_{\mathrm{act}}(o_t|m_i)=E_{\mathrm{base}}+\chi\left[-\ln p(o_t|m_i)\right]$$

或者在高斯预测编码近似下：

$$E_{\mathrm{act}}(o_t|m_i)=E_{\mathrm{base}}+\chi\,\varepsilon_t^\top\Pi_t\varepsilon_t$$

因此，记忆痕迹具有两个绑定在一起的效果。局部层面，重复刺激改变连接强度、兴奋性和阈值；宏观层面，由此形成的回路看起来像是在通过改善预测降低未来自由能，并且在物理上通过减少需要放电传递的误差信号降低未来代谢成本。

因此，长期记忆保留规则应被理解为局部可塑性的观察者层面摘要，而不是神经元自己的决策规则。定义一个有效记忆收益：

$$r_i^{mem,obs}(t)=\max(0,\Delta F_i^{obs}(t))-c_i(m_i,t)$$

其中 $c_i(m_i,t)$ 表示该记忆痕迹的持续维护成本。于是，从观察者层面看，保留该记忆的长期价值可以写成：

$$V_i^{obs}(t)=\mathbb{E}\left[\int_t^\infty e^{-\gamma(s-t)}r_i^{mem,obs}(s)ds\right]$$

因此，记忆保留的原则可以写成一种宏观等效关系：

$$\text{痕迹倾向于保持可复用}\quad\Longleftrightarrow\quad V_i^{obs}(t)>0$$

也就是：

$$\mathbb{E}\left[\int_t^\infty e^{-\gamma(s-t)}\,\Delta F_i^{obs}(s)\,ds\right]>C_{\text{maintain}}(m_i)$$

这意味着：

> 长期保持可复用的结构，并不是由神经元判断“未来收益”后选择出来的；它们是在局部生理机制中被反复激活、共激活和巩固下来的结构。从外部看，这些结构可以被描述为能够反复降低未来自由能、且维护成本低于收益的低代价路径。

这也解释了抽象为什么会出现。

抽象并不是额外附加的高级能力，而是记忆动力学在有限资源下的自然结果。当多个经验片段反复招募重叠回路时，这些回路会变得更容易被再激活，也更节能。在宏观层面，这表现为一个能跨情境降低自由能的结构。

可以定义抽象结构 $a$ 的观察者层面价值：

$$V^{obs}(a)=\sum_j w_j \Delta F_a^{obs}(e_j)-C_{\text{maintain}}(a)$$

当：

$$V^{obs}(a)>0$$

那么从建模者角度看，该抽象结构就表现为值得被保留。

所以：

> 抽象是被反复招募、局部强化、并逐渐变得更容易再激活的结构；在观察者层面，它等价于一种跨多个经验都能降低自由能、且维护成本低于总收益的压缩结构。

---

## 7. 大脑不是追求眼前最省力，而是追求长期低代价

一种常见误解是：大脑总是选择最简单、最省力的办法。

这个说法只对了一半。

更准确地说：

> 重复刺激倾向于留下那些更容易、更低能耗地被再激活的回路。从宏观建模角度看，这些回路就是长期累计自由能较低的回路。它们不是由神经元显式优化选择出来的，而是由可塑性、阈值适应和能量约束共同涌现出来的。

很多高级能力在形成初期都非常耗能：

- 学数学初期很费力；
- 学编程初期很费力；
- 学语言初期很费力；
- 建立抽象模型初期很费力。

但一旦稳定回路形成，未来处理同类问题时就会显著降低认知成本。

原因并不只是系统保存了一个符号化答案，而是学习后的回路改变了未来输入到预测误差的映射：

$$\varepsilon_C(t|m_C)=o_t-g_C(m_C,W_C)$$

因此也改变了未来激活成本：

$$E_C(t|m_C)=E_{\mathrm{base}}+\chi\,\varepsilon_C(t|m_C)^\top\Pi_C\varepsilon_C(t|m_C)$$

当 $m_C$ 和 $W_C$ 成为某类情境的更好先验时，$\varepsilon_C$ 会下降。长期路径之所以更省，是因为回路不再需要从零重新生成同一个推断。

因此，大脑不是单纯选择：

$$\text{当前最省力路径}$$

而是选择：

$$\text{长期累计代价较低的路径}$$

也就是：

$$C^{\ast}=\arg\min_{C}\int_{t_0}^{t_1}\left[F_C(t)+\lambda_E E_C(t)+\lambda_M M_C(t)+\lambda_C C_C(t)\right]dt$$

---

## 8. 过载、疲劳与注意力塌缩

设系统当前总代价率为：

$$L(t)=F_t+\beta \mathcal{G}_t+\lambda_E E_t+\lambda_A A_t+\lambda_M M_t+\lambda_C C_t$$

资源边界为：

$$R_{\text{total}}(t)$$

当：

$$L(t)<R_{\text{total}}(t)$$

系统可以维持稳定推理、记忆整合和行动控制。

但当：

$$L(t)\ge R_{\text{total}}(t)$$

系统进入过载状态。

此时，大脑可能采取保护性策略：

- 降低输入增益；
- 缩窄注意力范围；
- 退回熟悉回路；
- 降低抽象推理能力；
- 减少高阶整合；
- 表现出疲劳、脑雾、眩晕或回避反应。

可以引入有效温度：

$$T_{\text{eff}}(t)=T_0 \max\left(1,\dfrac{L(t)}{R_{\text{total}}(t)}\right)$$

当负载越接近或超过资源边界时，系统噪声增强，信念更新变得不稳定：

$$d\mu_t^k=-\Gamma_\mu\nabla_\mu F_t^k \, dt+\sqrt{2\Gamma_\mu T_{\text{eff}}(t)}\,dW_t^{\mu,k}$$

这可以作为认知过载的形式化模型。

不过需要谨慎：眩晕、疲劳和脑雾在真实生理中可能涉及前庭系统、血压、自主神经、代谢状态、炎症和睡眠等多种因素。因此，该模型不能被理解为对这些现象的唯一解释，而应被视为一种可检验的认知层解释假说。

---

## 9. 高阶观察者不是小人，而是元控制系统

在这个框架中，“高阶观察者”不应被理解为大脑里还有一个小人在观看一切。

更严格地说，它是一组调节低层推断过程的慢变量元控制系统。

可以定义元控制变量：

$$\theta_t=\{\lambda_E,\lambda_A,\lambda_M,\lambda_C,\Gamma_\mu,T_{\text{eff}},\kappa\}$$

它们调节：

- 不同资源代价的权重；
- 信念更新速度；
- 注意力精度；
- 探索噪声；
- 候选路径竞争温度；
- 高阶控制增益。

元控制目标可以写成：

$$J(\theta)=\mathbb{E}\left[\int_0^T \ell_t(\theta)\,dt\right]$$

元学习动力学为：

$$d\theta_t=-\eta_\theta\nabla_\theta J(\theta_t)\,dt+\sigma_\theta\,dW_t^{\theta}$$

因此，高阶观察者的严格版本是：

> 一个调节精度、成本权重、更新速度和有效温度的元控制系统。

它的功能不是神秘地“看见意识”，而是在多条候选认知路径之间重新分配有限资源，使系统尽量维持在长期低代价路径上。

---

## 10. 统一模型

综合以上部分，可以得到一个资源约束主动推理系统：

$$\begin{aligned}
d\mu_t^k &= -\Gamma_\mu\nabla_\mu F_t^k\,dt + \sqrt{2\Gamma_\mu T_{\text{eff}}(t)}\,dB_t^{\mu,k} \\
dz_t^k &= \frac{-z_t^k-\ell_t^k}{\tau_z}\,dt + \sigma_z\,dB_t^{z,k} \\
\rho_t^k &= \frac{\exp(z_t^k/T_{\mathrm{eff}})}{\sum_j \exp(z_t^j/T_{\mathrm{eff}})} \\
dm_i &= \left[-\lambda_i m_i + \alpha_\varepsilon \lVert\Pi_t\varepsilon_i\rVert + \alpha_V V_i(t) + \alpha_N\mathrm{Novelty}_i(t)+\alpha_c\,\mathrm{coact}_i(t)-\alpha_EE_i(t)\right]dt + \sigma_m\,dB_t^{m,i} \\
d\theta_t &= -\eta_\theta\nabla_\theta J(\theta_t)\,dt + \sigma_\theta\,dB_t^{\theta}
\end{aligned}$$

其中：

$$\begin{aligned}
\hat o_t^k &= G_k(m_t,\mathbf W_t^k) \\
\varepsilon_t^k &= o_t-\hat o_t^k \\
F_t^k &= \frac{1}{2}(\varepsilon_t^k)^\top\Pi_t^k\varepsilon_t^k+\frac{1}{2}\lambda_W\lVert\mathbf W_t^k\rVert^2+\mathrm{Complexity}_t^k \\
E_t^k &= E_{\mathrm{base}}+\chi(\varepsilon_t^k)^\top\Pi_t^k\varepsilon_t^k+E_{\mathrm{plastic}}^k(t) \\
\ell_t^k &= F_t^k + \lambda_E E_t^k + \lambda_A A_t^k + \lambda_M M_t^k + \lambda_C C_t^k-\lambda_V V_t^k-\lambda_{IG}IG_t^k
\end{aligned}$$

如果不显式建模路径 logit，也可以直接把路径选择写成负代价 softmax：

$$\rho_t^k=\frac{\exp(-\ell_t^k/T_{\mathrm{eff}})}{\sum_j\exp(-\ell_t^j/T_{\mathrm{eff}})}$$

使记忆真正成为先验的微观变量可以追加为：

$$\begin{aligned}
\dot{\mathbf W}_t^k &= \eta_W q_t^k\Pi_t^k\varepsilon_t^k\phi(m_t)^\top-\lambda_W\mathbf W_t^k+\sigma_W\xi_W^k \\
\tau_{\mathrm{th}}\,dV_{\mathrm{th},t}^k &= \kappa_{\mathrm{th}}\left(r_t^k-r_{\mathrm{target}}\right)dt
\end{aligned}$$

这套模型把以下闭环机制统一到一个框架中：

$$\text{有限资源}\rightarrow\text{神经积分}\rightarrow\text{预测误差}\rightarrow\text{可塑性与动态阈值}\rightarrow\text{先验}\rightarrow\text{未来误差和能耗降低}\rightarrow\text{认知路径代价}\rightarrow\text{多路径竞争}\rightarrow\text{记忆动力学}\rightarrow\text{抽象}\rightarrow\text{元控制}\rightarrow\text{适应性智能}$$

---

## 11. 潜在实验预测与可操作化

这个模型只有在能够连接到可观测量时，才具有科学价值。$\lambda_A,\lambda_C,\lambda_M$ 和 $T_{\mathrm{eff}}$ 不应被理解为可以直接读取的神经量，而应被理解为通过行为和生理数据拟合出来的潜变量。

可以提出几类可检验预测。

第一，在双任务负荷、时间压力、感官噪声或睡眠不足条件下，模型预测有效温度 $T_{\mathrm{eff}}$ 上升。候选路径选择会变得更随机，选择分布的熵会升高：

$$P(k)\propto \exp\left(-\frac{\mathcal{C}_k}{T_{\mathrm{eff}}}\right)$$

随着 $T_{\mathrm{eff}}$ 上升，选择分布应变平，反应时变异性应增加，系统也更容易依赖熟悉的低成本策略。

第二，当控制成本上升时，即使高控制策略更准确，系统也会减少对它的分配。这预测了更大的任务切换成本、更强的 Stroop 式干扰、更弱的工作记忆维持和更多习惯性反应。可用的经验代理指标包括反应时、错误率、主观努力评分、瞳孔直径、额中线 theta，以及 dACC/PFC 的 BOLD 活动。

第三，局部重复保留规则预测：反复共激活的痕迹更容易保持可复用，并在之后被重新激活，尤其是当它们能改善之后的预测或行动时。这可以通过延迟回忆、再认、睡眠后巩固、表征重激活和迁移任务来检验。建模者可以把成功留下来的痕迹描述为降低了未来自由能，但生物机制是局部可塑性和巩固，而不是显式计算未来价值。

第四，抽象规则预测：多个任务之间共享结构越强，系统越倾向形成压缩表征。在资源压力下，抽象深度应下降，被试会更多依赖具体熟悉样例，而较少调用高阶规则。在计算模型中，这应表现为从高层潜变量向低层缓存策略的转移。

这些预测不能证明真实大脑精确最小化 $\mathcal{C}_{path}$，但它们让模型具有可证伪性：如果负荷、控制成本、重复刺激、巩固条件或记忆价值的操纵没有按照预测方向改变选择熵、控制分配、记忆保留或抽象水平，模型就需要被修正。

---

## 12. 哪些可以严格证明，哪些仍是假说？

这个框架中有一部分是严格的数学结果：

| 命题 | 状态 |
|---|---|
| 变分自由能上界负对数证据 | 可证明 |
| 最小化自由能等价于逼近贝叶斯后验 | 可证明 |
| 资源约束推出拉格朗日路径泛函 | 可证明 |
| 权重 softmax 来自累计代价 | 可证明 |
| 连续极限给出复制器方程 | 可证明 |
| logits 保证权重合法 | 可证明 |
| 梯度流使自由能单调不增 | 在给定条件下可证明 |
| 观察者层面的记忆保留阈值规则 | 在净收益建模下可推出 |
| 抽象结构保留条件 | 可由观察者层面的记忆规则推广得到 |

但也有一些部分不能被直接证明，只能作为可检验假说：

| 主张 | 状态 |
|---|---|
| 真实大脑精确最小化该认知路径代价泛函 | 假说 |
| 能量、注意力、记忆和控制成本的具体形式唯一正确 | 假说 |
| 高阶元控制等同于某个具体脑区功能 | 需要实验支持 |
| 有效温度精确表示认知过载 | 有效模型 |
| 眩晕完全由认知负载超过资源边界导致 | 不能这样断言 |
| 记忆系统精确计算未来自由能收益 | 不能作为字面生物机制 |
| 抽象一定等于低自由能压缩结构 | 强理论假说 |

因此，本文的定位不是“证明大脑就是这样”，而是提出：

> 一个可计算、可模拟、可产生预测的规范性认知模型。

---

## 13. 最终命题

可以把整个理论浓缩为一句话：

> 大脑并不是在无限计算中寻找真理，而是在有限资源下通过局部神经动力学进入低作用量的认知路径；神经放电积分提供微观动力学，重复激活和可塑性提供记忆机制，自由能最小化提供观察者层面的误差降低描述，而高阶观察者作为元控制系统，在多条候选路径之间分配资源。当认知负载超过系统边界时，低作用量路径难以维持，系统便可能表现为疲劳、注意力塌缩、高阶整合失败和认知不稳定。

更学术化地说：

> 大脑可以被理解为一种资源约束下的认知路径优化系统：在有限能量、有限注意力、有限时间和有限记忆容量下，局部神经动力学会使反复有用的感知、记忆与行动路径更容易被再激活；在宏观层面，这些路径可以被建模为最小化累计路径代价和变分自由能、同时维持自我-环境适应性耦合。

这就是“资源约束认知路径优化框架”的核心。

---

## 参考文献

Friston, K. (2010). The free-energy principle: a unified brain theory? *Nature Reviews Neuroscience*, 11, 127-138.

Lieder, F., & Griffiths, T. L. (2020). Resource-rational analysis: Understanding human cognition as the optimal use of limited computational resources. *Behavioral and Brain Sciences*, 43, e1.

Parr, T., Pezzulo, G., & Friston, K. J. (2022). *Active Inference: The Free Energy Principle in Mind, Brain, and Behavior*. MIT Press.

Rissanen, J. (1978). Modeling by shortest data description. *Automatica*, 14(5), 465-471.

Schmidhuber, J. (2009). Driven by compression progress: A simple principle explains essential aspects of subjective beauty, novelty, surprise, interestingness, attention, curiosity, creativity, art, science, music, jokes. *Dagstuhl Seminar Proceedings*.

Shenhav, A., Botvinick, M. M., & Cohen, J. D. (2013). The expected value of control: An integrative theory of anterior cingulate cortex function. *Neuron*, 79(2), 217-240.
